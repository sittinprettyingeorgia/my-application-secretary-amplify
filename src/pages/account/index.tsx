import Wrapper from '@/shared/Wrapper';

const Account = () => {
  return <Wrapper></Wrapper>;
};

export default Account;

/** //TODO:
 * 1. Account needs a way to add/modify payment preferences.
 * 2. Account needs a way to cancel/upgrade subscription.
 * 3. Account needs a way to add/modify email preferences/ personal information, etc.
 **/
